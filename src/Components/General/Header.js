// Page Header Component

import react from "react";

const Header = (props) => {
    return (
        <div className="page-header">
            This is the header.
        </div>
    )
}

export default Header;