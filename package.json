{
  "name": "waldo",
  "version": "0.1.0",
  "private": true,
  "homepage": "https://brenton-j-andrews.github.io/Waldo/",
  "dependencies": {
    "cra-template": "1.2.0",
    "firebase": "^9.8.2",
    "react": "^18.1.0",
    "react-dom": "^18.1.0",
    "react-outside-click-handler": "^1.3.0",
    "react-router-dom": "^6.3.0",
    "react-scripts": "^2.1.3"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test",
    "eject": "react-scripts eject",

    "predeploy": "npm run build",
    "deploy": "gh-pages -d build"
  },
  "eslintConfig": {
    "extends": [
      "react-app",
      "react-app/jest"
    ]
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  },
  "devDependencies": {
    "gh-pages": "^4.0.0"
  }
}
